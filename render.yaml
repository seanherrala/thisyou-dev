services:
  - type: web
    name: thisyou-dev
    runtime: python
    plan: free
    buildCommand: pip install .
    startCommand: uvicorn thisyou.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
